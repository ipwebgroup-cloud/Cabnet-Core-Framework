Cabnet Core Framework v3.0.0 — Validation and Form Metadata Convergence
Patch type: overlay patch (changed files only)
Base expected: v2.9 module-registry adoption state

Apply by copying these files over the existing framework tree, preserving paths.

Included changes:
- CRUD field metadata now derives validation rules
- shared DefinitionCrudService added
- ServiceCrudService reduced to a thin definition-driven wrapper
- CRUD form partial now renders metadata-driven attributes/help text
- CRUD generator now emits richer field metadata and definition-driven services
- validator now supports in: rules and works without mbstring
- smoke suite expanded and passing: 27/27
