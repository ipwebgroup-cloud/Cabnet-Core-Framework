# Built-in Blueprint Examples

These example blueprints can be used directly with the generator scripts via `example:<name>`.

Available examples:
- `content-pages`
- `media-assets`
- `localized-services`
