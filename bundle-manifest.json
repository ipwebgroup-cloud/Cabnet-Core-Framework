{
  "bundle": "Cabnet Core Framework",
  "version": "4.1.0",
  "release_name": "Relation Filter Option Hydration",
  "package_mode": "patch_only",
  "notes": [
    "Includes only files changed for v4.1.",
    "Overlay this patch on top of the current framework tree.",
    "Relation-backed list filters can now hydrate select options from relation metadata at runtime.",
    "Src-first scaffold output now preserves relation-backed derived filters as select controls even when options are database-backed."
  ]
}
