{
  "bundle": "Cabnet Core Framework",
  "version": "3.4.0",
  "release_name": "Module Permissions and Filter Metadata",
  "package_mode": "patch_only",
  "notes": [
    "Includes only files changed for v3.4.",
    "Overlay this patch on top of the current framework tree.",
    "config/modules.php now drives module action roles and list-filter metadata.",
    "BaseCrudController now enforces module permissions and shared CRUD list views now render metadata-driven filters."
  ]
}
